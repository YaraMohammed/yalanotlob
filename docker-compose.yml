version: '2'
services:
  web:
    build: .
    depends_on:
      - db
    links:
      - db
    ports:
      - 8888:8888
  db:
    image: mongo
    volumes: 
      - ~/data:/data/db mongo
    ports:
      - 27017:27017
